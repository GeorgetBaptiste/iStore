package com.istore.controller;

import com.istore.model.AbstractModel;

public class ArticleController extends AbstractController{

    public ArticleController(AbstractModel model) {
        super(model);
    }
}
